export class Film {
  title: string;
  director: string;
  // tslint:disable-next-line:variable-name
  release_date: string;
  id: string;
  // tslint:disable-next-line:variable-name
  episode_id: number;
}
